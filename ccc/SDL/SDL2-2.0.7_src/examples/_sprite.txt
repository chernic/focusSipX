SDLTest_CommonCreateState

SDL_RenderCopy(
  SDL_Renderer *renderer: 渲染目标
  SDL_Texture  *texture:  输入纹理
  SDL_Rect     *srcrect:  选择输入纹理的一块矩形区域作为输入
                          设置为NULL的时候整个纹理作为输入
  SDL_Rect     *dstrect:  选择渲染目标的一块矩形区域作为输出
                          设置为NULL的时候整个渲染目标作为输出
)
